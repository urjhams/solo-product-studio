# Mobbin adapter

Optional reference provider. Search by interaction problem, record successful references, critique and adapt patterns, and fall back to `pattern-library/` plus platform guidance when unavailable.
