# Design Contract Validator

Check promise, magic moment (reachable from a cold start inside the onboarding path, and attributable to the product), onboarding path with what each step costs and earns, flow, screen/state inventory, exactly three actionable principles, landing/store copy with every line traceable to a Define slot, design system covering type/spacing/color roles/component inventory, cut list, relevant loading/empty/error states, timebox fit, accessibility, and platform consistency. Confirm the Design Prompt exists at `.product-studio/artifacts/design-prompt.md`, and that `design.canvas_provider` names only a provider that actually ran.

Prototype and Hackathon cut the landing/store copy and the full design system — check they are on the cut list, not silently absent. See `references/design-loop.md`.
