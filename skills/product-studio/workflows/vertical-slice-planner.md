# Vertical Slice Planner

Break the build into user-visible slices, each with testable value, dependencies, acceptance criteria, and an explicit mock/real boundary. Reject horizontal layer-only plans.
