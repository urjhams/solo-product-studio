interface:
  display_name: "Workflow Init"
  short_description: "Scaffold the agent workflow, gates, and CI into a repo"
  default_prompt: "Use workflow-init to set up the agent workflow in this repo."
