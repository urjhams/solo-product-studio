# Project state — read me first

> Answers "where are we right now?". Update in every task-completing commit: **ONE compact
> ≤2-line bullet** under "## Current focus", newest first — date, `branch/PR`, one-clause
> outcome, needs-eyeball / evaluated-✓ flags. Detail lives in git history + PR bodies, never
> here. **Caps: 8 bullets AND ≤3,000 bytes for the whole "Current focus" section** — measure
> bytes, not just bullets. Compaction is mechanical: at either cap,
> `python3 scripts/agent/compact-state.py` folds the oldest bullets verbatim into
> [`STATE-archive.md`](STATE-archive.md) (demote, never delete) — on Claude Code a PostToolUse
> hook runs it automatically when this file is edited. Merge conflict between adjacent bullets
> from parallel PRs = keep both.

## Current focus

- {{DATE}}: workflow initialized by workflow-init.

## Next

-

## Active constraints

-
