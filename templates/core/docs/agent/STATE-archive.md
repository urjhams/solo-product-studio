# Project state — archive

Compacted one-liners, newest first, demoted from [`STATE.md`](STATE.md). STATE.md is read at the
top of every session, so it holds only what answers "where are we *right now*" — this is the
reference tail. Read it only when you need the history of a specific branch or issue. Full detail
lives in git history and PR bodies.

<!-- compact-state inserts below -->
