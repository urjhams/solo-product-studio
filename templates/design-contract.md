# Design Contract

## Promise and magic moment
- Product promise:
- Magic moment (the hero moment — what the user sees when the promise lands):
- Why it is attributable to the product:
- Primary experience spine:

## Onboarding path
- Cold start to magic moment, one step per screen:
  1. — asks for: — earns the user:
  2. — asks for: — earns the user:
  3. — asks for: — earns the user:
- Steps deliberately moved after the magic moment:
- Permissions and when each is asked:

## Flow and scope
- Core user flow:
- Screen inventory:
- State inventory:
- Information hierarchy:
- Cut list:

## Landing / store listing
- Headline (the Outcome, in the customer's words):
- Subhead (the Mechanism, one line):
- Proof point 1 (cite the Proof slot):
- Proof point 2:
- Proof point 3:
- Primary CTA:
- Top objection + answer:
- App Store — name / subtitle / promo text:
- App Store — one caption per screenshot:

## Visual brief
```yaml
visual_brief:
  desired_feeling:
  visual_metaphor:
  emotional_tone:
  content_density:
  contrast_level:
  image_treatment:
  motion_character:
  signature_element:
  avoid: []
```

## Exactly three design principles
1.
2.
3.

## Design system
- Type scale:
- Spacing scale:
- Color roles (surface / content / accent / state):
- Component inventory (only what the screen list uses):
- Signature component:
- Motion plan:

## Implementation contract
- Accessibility requirements:
- Platform mapping (native / cross-platform / web):
- Design prompt: `.product-studio/artifacts/design-prompt.md`
- Canvas provider (empty when none ran) / URL:
- Acceptance criteria: (design-level only; the Behavior Spec produced in the Specify phase supersedes these for implementation)
