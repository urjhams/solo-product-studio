# Adding an internal agent

Add a Markdown role contract under `skills/product-studio/agents/` with ownership, inputs, outputs, boundaries, and handoffs. Reference it from the orchestrator only when the current stage requires it. Do not add a second public `SKILL.md`; the public surface remains `product-studio`.
