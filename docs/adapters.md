# Adapters

Adapters are optional capability providers. Web research must preserve citations and dates. UX adapters must record references and never claim unavailable providers were used. GitHub providers are tried in order: connected integration, `gh`, local export. Every adapter has a bundled fallback.
