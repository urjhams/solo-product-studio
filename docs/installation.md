# Installation

Use `scripts/install.py` with `--target codex`, `--target claude-code`, `--target opencode`, or `--target agents`. Add `--destination` for project-local installs and `--symlink` for development. Add `--uninstall` to remove only the installed skill directory. See the root README for commands and host-specific paths.
