# Workflow

The lifecycle is QA-first: intake → stage/mode recommendation → user confirmation → stage-specific questions → draft artifact → review → completion gate → next action. Stage and mode remain independent. Hackathon, Indie, SaaS, Startup, and Production paths optimize different MVP and planning decisions.
