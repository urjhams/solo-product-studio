# Architecture

`product-studio` is the only public skill. Its body is the orchestration contract. Internal agent roles, core capabilities, supporting workflows, schemas, templates, pattern libraries, and optional adapters are resources loaded progressively. Local YAML state is the canonical cross-session memory; Markdown artifacts are the human-reviewable outputs.
