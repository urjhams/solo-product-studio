# Quick prototype to validate an idea

Input: `I want a quick MVP of an app that matches people with running partners — just want to see if the idea works.`

Expected recommendation: Prototype Mode. The QA session narrows to one flow, confirms the mock boundary with the user (fake profiles and matches, no auth, no backend), picks Expo over native iOS on setup speed, produces a 3–6 step build plan with one runnable check on the matching logic and a click-through acceptance path, and ends by asking what the prototype showed before re-running mode selection.
