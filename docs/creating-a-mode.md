# Creating an operating mode

Add the mode to `references/operating-modes.md`, define its optimization priority, recommendation signals, QA questions, MVP rules, artifact adaptations, and exit criteria. Add a routing test and preserve user override through Custom Mode.
